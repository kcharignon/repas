<div class="form-check form-switch float-end" id="switchShoppingListActiveGroup">
    <input
        data-action="btn-ajax"
{#        data-url="{{ path("view_shopping_list_locked", {"id" : shoppingList.id, "isLocked" : (shoppingList.isPlanning ? "0" : "1")}) }}"#}
        data-url="{{ (shoppingList.isPlanning ? path("view_shopping_list_shopping", {"id" : shoppingList.id}) : path("view_shopping_list_planning", {"id" : shoppingList.id})) }}"
        class="form-check-input"
        type="checkbox"
        id="switchShoppingListActive" {{ shoppingList.isPlanning ? "checked" : ''}}>
    <label class="form-check-label" for="switchShoppingListActive">En cours</label>
</div>

<nav class="navbar navbar-expand navbar-dark bg-dark" style="position: fixed; width: 100%; z-index: 10;">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><img src="{{ absolute_url(asset("images/logo.png")) }}" alt="logo de repas" style="height: 1.5em;"></a>
        {# Bouton toggler supprimé car inutile si le menu est toujours affiché #}
        <div class="navbar-collapse">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? '') == 'recipe') ? 'active' : '' }}" href="{{ path('view_recipes') }}">Recettes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? '') == 'department') ? 'active' : '' }}" href="{{ path('view_departments') }}">Rayons</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ ((active ?? '') == 'shoppingList') ? 'active' : '' }}" href="{{ path('view_shopping_list') }}">Liste</a>
                </li>
                {% if app.user and app.user.isAdmin %}
                <li class="nav-item">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Admin
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('view_admin_conversions') }}">Conversions</a></li>
                            <li><a class="dropdown-item" href="{{ path('view_admin_departments') }}">Rayons</a></li>
                            <li><a class="dropdown-item" href="{{ path('view_admin_units') }}">Unités</a></li>
                            <li><a class="dropdown-item" href="{{ path('view_admin_users') }}">Utilisateurs</a></li>
                        </ul>
                    </li>
                </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-user-circle"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="{{ path('view_user_settings', {id: current_user.getUser().id}) }}">Parametres</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">Déconnexion</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
